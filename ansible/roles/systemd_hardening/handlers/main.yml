---
# roles/systemd_hardening/handlers/main.yml

- name: restart journald
  ansible.builtin.systemd:
    name: systemd-journald
    state: restarted

- name: reload systemd daemon
  ansible.builtin.systemd:
    daemon_reload: true
